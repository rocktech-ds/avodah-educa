# =============================================================================
# GOOGLE OAUTH ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.oauth and set your actual Google OAuth credentials
# Then source it before running OAuth configuration scripts:
# 
#   cp scripts/.env.oauth.example scripts/.env.oauth
#   # Edit scripts/.env.oauth with your actual credentials
#   source scripts/.env.oauth
#   ./scripts/configure-google-oauth.sh

# Google OAuth 2.0 Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id-here.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret-here"

# VPS Configuration
VPS_HOST="your.vps.ip.address"
VPS_USER="root"

# Optional: Domain Configuration
AUTH_DOMAIN="your-auth-domain.com"
API_DOMAIN="your-api-domain.com" 
STUDIO_DOMAIN="your-studio-domain.com"

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. NEVER commit .env.oauth to version control
# 2. Keep your Google OAuth credentials secure
# 3. Restrict OAuth redirect URIs in Google Console
# 4. Use different credentials for development vs production
# 5. Regularly rotate your OAuth client secrets
# 
# =============================================================================

# Example usage:
# 1. Set up credentials:
#    source scripts/.env.oauth
# 
# 2. Run OAuth configuration:
#    ./scripts/configure-google-oauth.sh
# 
# 3. Or run VPS diagnosis:
#    ./scripts/diagnose-vps-supabase.sh